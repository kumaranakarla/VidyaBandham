// Used for the production build (Netlify). Update this to your actual Render
// backend URL after you deploy the backend, then redeploy the frontend
// (a `git push` will trigger Netlify to rebuild automatically if it's
// connected to your GitHub repo).
export const environment = {
  apiUrl: 'https://YOUR-RENDER-BACKEND-URL.onrender.com/api',
};
